namespace Exercícios;

class Program
{
    static void Main(string[] args)
    {
        // Atividade - Funções + Arrays

        // NÍVEL 1

        // Atividade 1 — Contar letras específicas
        // string[] nomes = {"Penelope", "Davi", "Iago"};
        // static int qLetras (string[] nomes) 
        // {
            
        // }

        // Atividade 2 — Mostrar nomes ao contrário
        string[] nomes = {Ana, Bruno, Carlos, Duda};
        Array.Reverse(nomes);
        Console.WriteLine(string.Join(", ", nomes));
    }
}
